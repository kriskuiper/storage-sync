describe('Can get item from storages', () => {
    test('Gets item from localStorage', () => {
        return Promise.resolve()
    })

    test('Gets item from sessionStorage', () => {
        return Promise.resolve()
    })
})

describe('Can set items in storages', () => {
    test('Sets item in localStorage', () => {
        return Promise.resolve()
    })

    test('Sets item in sessionStorage', () => {
        return Promise.resolve()
    })
})